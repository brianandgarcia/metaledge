<ips:template parameters="$lang, $id=NULL, $css='', $parse=FALSE" />
<li{{if $id !== NULL}} id="{$id}"{{endif}}>
	<div {{if $css}}class="ipsMessage ipsMessage_{$css}"{{endif}}>
		{{if $parse}}
			{lang="$lang"}
		{{else}}
			{$lang|raw}
		{{endif}}
	</div>
</li>