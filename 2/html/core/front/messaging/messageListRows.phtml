<ips:template parameters="$conversations, $pagination=NULL, $overview=FALSE" />
{{if empty( $conversations )}}
	<li class='ipsDataItem'>
		<div class='ipsPad ipsType_light ipsType_center ipsType_normal'>{lang="no_results_messages"}</div>
	</li>
{{else}}
	{{foreach $conversations as $row}}
		{template="messageListRow" group="messaging" params="$row, $overview"}
	{{endforeach}}
{{endif}}