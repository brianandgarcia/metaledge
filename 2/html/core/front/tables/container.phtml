<ips:template parameters="$table, $header=NULL" />
<div class='ipsPad'>
	<h1 class='ipsType_pageTitle'>{{if $header}}{$header}{{else}}{expression="\IPS\Output::i()->title"}{{endif}}</h1>
	<br>
	{$table|raw}
</div>